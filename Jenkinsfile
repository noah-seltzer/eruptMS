pipeline {
  agent {
    dockerfile {
      dir 'COMP4911Timesheets'
      filename 'build.Dockerfile'
      
    }

  }
  stages {
    stage('build container') {
      steps {
        sh ''
      }
    }
  }
}
